Speare {
  Program       = (Work | Statement)*
  Work          = Corollary | Composition
  Composition   = "Composition" Varname "{" Work* "}"                           --class
  Corollary     = enter type Varname "(" ListOf<Param, ","> ")" "{" Body "}"    --function
  Body          = (Statement | Work)*
  Statement     = ContFlow+
                | alloweth type Varname "be" RelExp                             --variable
                | Varname "be" (RelExp)                                         --assignment
                | "speaketh" "(" RelExp ")"                                     --print
                | "returneth" (Varname | RelExp)                                --return
                | Varname (incrementby | decrementby) RelExp                    --incdecby
                | Varname (increment | decrement)                               --incdec
  ContFlow      = if "(" LogicExp+ ")" "{" Body "}" ( elif "(" LogicExp+ ")" "{" Body "}" )* ( else "{" Body "}")? --complexconditional
                | if "(" LogicExp+ ")" "{" Body "}" ( elif "(" LogicExp+ ")" "{" Body "}" )*                       --semicomplexconditional
                | if "(" LogicExp+ ")" "{" Body "}"                                                            --simpleconditional
                | switch Factor "{" (case Factor ":" Body break?)+ "}"          --switchcase
                | for "(" Statement "," (Statement | RelExp) "," Statement? ") {" Body break? "}"      --forloop
                | for "(" Varname in Varname ") {" Body break? "}"              --forin
                | while "(" LogicExp+ ") {" Body break? "}"                     --while
                | do "{" Body break? "}" while "(" LogicExp+ ")"                --dowhile
  LogicExp      = LogicExp logicalop RelExp                                       --logicalcombo
                | RelExp
  RelExp        = AddSub relop AddSub                                           --equality
                | AddSub                                                        --defer
  AddSub        = AddSub (subtraction | addition) MultDiv                       --addorsubtract
                | MultDiv                                                       --defer
  MultDiv       = MultDiv (multiplication | division | modulus) Exponentiate    --multordiv
                | Exponentiate
  Exponentiate  = Factor (exponentiate) Exponentiate                            --raisepower
                | Factor
  Factor        = Varname
                | StructureType                                                 --Types
                | "(" AddSub ")"                                                --parens
                | ("nay" | abs | sqrt)	"(" Factor ")"                          --unary
                | String
  DictItem      = AddSub colon AddSub                                           --dictionaryitem
  
  
  
  // Define ids for variables, params
  Varname             = id                             --string

  Param               = type Varname
  
  //Reserved keywords
  alloweth            = "alloweth" ~alnum
  enter               = "enter"
  colon               = ":"

  type	              = "Liste of " type ~alnum                                  --listdec
                      | "Concordance of " type " and " type                        --dictdec
                      | "Indistinguishable"
                      | "ToBeOrNotToBe"
                      | "Numeral"
                      | "Lexicographical"
                      | "Illused"
                      | "Ideogram"
                      | "Corollary"

  StructureType       = Indistinguishable
                      | Tobeornottobe
                      | Numeral
                      | Lexicographical
                      | Illused
                      | Liste
                      | Concordance

  //Data Structures                                  
  dsType              = "Liste"
                      | "Concordance"

  // Type
  id                  = ~keyword letter alnum*            --name
  String              = ~keyword "\"" letter* "\""        --string
  Indistinguishable	  = "indistinguishable" ~alnum
  Tobeornottobe	   	  =	"fallacious" | "faithful"                               --binary
  Numeral             = digit+ ("." digit+)?
  Liste               = "[" ListOf<AddSub, ","> "]"
  Concordance         = "{" ListOf<DictItem, ","> "}"

  Lexicographical     = "\"" alnum "\""
  Illused             = "null"

  // Operations
  addition            = "with"
  subtraction         = "without"
  multiplication      = "accumulate"
  division            = "sunder"
  modulus             = "residue"
  exponentiate        = "exponentiate"
  abs                 = "absolutization" ~alnum
  sqrt                = "quadrangle" ~alnum
  composition         = "Composition" ~alnum
  
  // Relational and Logical operations
  relop               = "tis nobler"
                      | "tis not"
                      | "tis lesser"
                      | "tis"
                      | "nobler"
                      | "lesser"
  logicalop           = "furthermore"
                      | "alternatively"
                      | "nay"
  
  //Incrementing
  assignment		      = "be"
  increment           = "increment"
  incrementby         = "incrementby"
  decrement           = "decrement"
  decrementby         = "decrementby"
  
  // Control Flow
  if                  = "whether"
  elif                = "subsequently"
  else                = "otherwise"
  switch              = "trigger"
  case                = "condition"
  break               = "exit"
  while               = "whilst"
  do                  = "execute"
  for                 = "in regards to"
  in                  = "within"
  
  controlflow         = elif
                      | for
                      | if
                      | else
                      | switch
                      | case
                      | break
                      | while
                      | do
                      | in
  
  // Defining symbols
  ideogram            = addition
                      | subtraction
                      | multiplication
                      | division
                      | modulus
                      | assignment
                      | relop
                      | logicalop
                      | sqrt
                      | abs
                      | increment
                      | decrement
  
  // Defining Objects
  accommodation	      = type
   
   //Defining Reserved Words
   keyword            = ideogram ~alnum
                      | alloweth ~alnum
                      | accommodation ~alnum
                      | composition ~alnum
                      | controlflow ~alnum
  	
}